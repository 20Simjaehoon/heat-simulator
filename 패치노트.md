# 패치노트

---

# v2.18

## 데이터 품질 검사 추가
기존에는 열지침량이 통째로 비어 있는 달이 있어도 아무 안내 없이 그 달을 조용히 건너뛰고 결과를 냈습니다(예: 광교레이크시티 파일의 2025년 12월). 이제 월별 누락 비율을 검사해 두 단계로 알립니다.

- **계산 불가능 (붉은 오류 배너, 해당 월 제외)**
  한 달의 열지침량 누락이 **50% 이상**이거나 유효 데이터가 2건 미만이면 그 달을 결과에서 제외하고, 어떤 달이 왜 빠졌는지 누락 시간 수와 비율을 함께 표시합니다. 제외된 달의 레코드는 계산에서 완전히 배제해 남은 조각이 다른 달로 잘못 보간되지 않도록 했습니다. 상단 상태 줄에도 "(N개월 제외됨)"이 함께 표기됩니다.
- **계산 부정확 (노란 경고 배너, 계산에는 포함)**
  누락이 **5% 이상 50% 미만**이면 계산은 진행하되 해당 월과 누락 비율을 경고로 안내합니다.
- **모든 달이 계산 불가능한 경우**
  결과를 내지 않고 "데이터 누락이 심해 계산할 수 없습니다 — 2025년 12월(누락 100%), …" 형태의 오류 메시지만 표시합니다.

기존 수요관리시간대 보간 경고는 그대로 유지되며, 위 경고와 같은 배너에 함께 표시됩니다.

**검증 결과** (실제 파일 기준)

| 파일 | 판정 |
|---|---|
| 광교레이크시티(null값) | 2025-12(100% 누락)·2026-01(58% 누락) 제외, 2026-02만 계산 |
| 예시(건물번호90) | 2026-01(11.1% 누락) 경고 후 계산 포함 |
| CJ R&D CENTER 3개, IM센터 | 이상 없음 — 기존과 동일하게 전월 계산 |

---

# v2.17

## 추가 기능
- **연도별 과거 요금 관리: 연도 선택을 드롭다운으로 변경**
  텍스트로 고정되어 있던 동절기 연도를 셀렉트박스로 바꿔 클릭 한 번으로 변경 가능. 이미 사용 중인 연도는 목록에서 자동 제외되어 중복 선택 불가.
- **연도별 과거 요금 관리: "＋ 연도 추가" 버튼 추가**
  표 하단에 버튼을 추가해 행을 계속 늘릴 수 있음. 새로 추가된 행은 요금값이 전부 빈칸으로 시작하고, 직접 입력한 값만 검정색으로 표시(미입력 샘플값과 구분).

## UI 수정
- **연도별 요금 관리 표의 행 높이 정렬 수정**
  연도 셀 아래 기간 표기(`2025.12~2026.2`)로 인해 셀렉트박스·입력창·삭제버튼의 세로 축이 어긋나던 문제 수정. 모든 셀이 동일한 상단 기준선과 높이(30px)로 정렬되도록 함.

## 계산 로직 버그 수정
- **엑셀 파싱: 24시(마지막 시간) 데이터 누락 수정**
  기존엔 `시각 > 23`인 행을 전부 버려서, 각 파일의 마지막 날(예: 2월 29일) 23~24시 사용량이 통째로 누락되고 있었음. 24시 데이터를 포함하도록 수정해 이 문제 해결. 부수 효과로 매달 말일 23~24시 사용량이 다음 달로 잘못 새어 들어가던 월별 집계 오차도 함께 바로잡힘.

- **엑셀 파싱: 계량기 지침값 스파이크로 인한 사용량 과다 계산 수정**
  지침값이 순간적으로 튀었다가 바로 돌아오는 구간(예: 2977.7 → 2967.8)에서, 상승분은 그대로 반영되고 하락분(마이너스)만 0으로 잘려 튄 만큼이 그대로 사용량에 더해지던 문제 수정. 하락폭이 직전 검침량의 50% 미만이면 마이너스를 그대로 반영해 스파이크를 상쇄하고, 50% 이상(계량기 교체로 판단)일 때만 0 처리. 검증 데이터 기준 2024년 2월 154.0 → **142.0 Gcal**로 정상화, 나머지 월은 영향 없음.

- **엑셀 파싱: 열지침량이 "null"(빈 값)인 시간대 보간 처리 추가**
  기존엔 열지침량 셀이 "null" 텍스트거나 비어 있으면 그 행 전체가 버려졌고, 그 여파로 앞뒤 정상 레코드 사이의 시간차가 1시간을 넘어가면서 **공백 시간뿐 아니라 바로 다음 시간의 사용량까지 함께 0 처리**되어 조용히 누락되던 문제가 있었음. 이제 공백 앞뒤의 정상 지침값 차이를 공백 시간 수만큼 균등 분배(보간)해 채움. 24시간을 초과하는 공백(계량기 교체·동절기 경계 등)은 신뢰할 수 없다고 보고 기존처럼 0 처리 유지. 실제 예시 파일(null 114개 포함) 기준 2026년 1월 686.2 → **815.8 Gcal**, 2월 455.6 → **509.3 Gcal**로 복구.
  - 보간된 구간이 수요관리시간대(07~10시)와 겹치면 업로드 결과 화면에 노란색 경고 배너로 해당 날짜·시간을 나열해 표시(실제 요금과 차이가 날 수 있음을 안내).
